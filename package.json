{
  "name": "commune-hub",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "release:patch": "npm run build && npm version patch -m \"chore(release): %s\" && git push --follow-tags"
  },
  "devDependencies": {
    "@wordpress/element": "^6.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {}
}
